# Copy to .env and fill in before running: docker compose -p infra up -d

# PostgreSQL superuser password. Each app gets its own user with a separate password.
POSTGRES_SUPERUSER_PASSWORD=change-me-strong-password
